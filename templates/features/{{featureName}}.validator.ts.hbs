import zod from 'zod';
import { AddUpdate{{titleCase featureName}}Model } from './{{featureName}}.model';
import { ValidatorType } from '~/helpers/shared-model';

export const addUpdate{{titleCase featureName}}Schema = zod.object<ValidatorType<AddUpdate{{titleCase featureName}}Model>>({

});
