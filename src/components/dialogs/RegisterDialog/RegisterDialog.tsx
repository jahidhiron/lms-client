import RegisterForm from '~/components/pages/auth/RegisterForm';

type RegisterDialogProps = {};

export default function RegisterDialog({}: RegisterDialogProps) {
  return (
    <div>
      <RegisterForm />
    </div>
  );
}
