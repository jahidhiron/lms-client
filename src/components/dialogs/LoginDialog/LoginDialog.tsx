import LoginForm from '~/components/pages/auth/LoginForm';

type LoginDialogProps = {};

export default function LoginDialog({}: LoginDialogProps) {
  return (
    <div>
      <LoginForm />
    </div>
  );
}
