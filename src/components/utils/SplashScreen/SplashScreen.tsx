import classes from './SplashScreen.module.css';

type SplashScreenProps = {};

export default function SplashScreen({}: SplashScreenProps) {
  return <div className={classes.splash}>loading...</div>;
}
